import React from 'react';
import { storiesOf } from '@storybook/react';

// components
import Pricing from '../Pricing';

// Pricing section
storiesOf('Pricing', module).add('show Pricing', () => <Pricing />);
