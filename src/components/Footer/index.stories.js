import React from 'react';
import { storiesOf } from '@storybook/react';

// components
import Footer from '../Footer';

// Footer section
storiesOf('Footer', module).add('show Footer', () => <Footer />);
